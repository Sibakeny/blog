<div class="container">
    <%= link_to '編集', edit_article_path(@article), class: 'btn btn-success btn-sm' %>

    <p>
        <strong>Title:</strong>
        <%= @article.title %>
    </p>

    <div>
        <%= markdown(@article.body).html_safe %>
    </div>
</div>